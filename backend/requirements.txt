fastapi>=0.115,<1
uvicorn>=0.34,<1
sqlalchemy>=2.0,<3
aiosqlite>=0.20,<1        # 测试/本地模式 (sqlite+aiosqlite)
asyncpg>=0.30,<1
pgvector>=0.3,<1
redis>=5.2,<6
PyJWT>=2.8,<3
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
httpx>=0.28,<1
numpy>=2.0,<3
sse-starlette>=2.2,<3
aiokafka>=0.12,<1
jsonschema>=4.23,<5         # Flink↔Python 跨运行时 Schema 校验
prometheus-client>=0.21,<1  # /metrics 指标出口 (轻量, 兼容 FastAPI 0.14x)
# OpenTelemetry 标准 trace (→ otel-collector → Grafana Tempo)
opentelemetry-api>=1.27,<2
opentelemetry-sdk>=1.27,<2
opentelemetry-exporter-otlp>=1.27,<2
opentelemetry-instrumentation-httpx>=0.48b0,<1
paramiko>=3.4,<4
cryptography>=42.0,<44
# ── NDR 扩展依赖 ──
scapy>=2.5,<3              # 网络流量采集与协议解析
aiohttp>=3.9,<4            # 异步 HTTP 客户端（TAXII/MISP/CAPE API）
geoip2>=4.8,<5             # GeoIP 地理位置查询（可选，需 MaxMind 数据库）
# ── 可选: TLS 解密代理 (encrypted_traffic/mitm_proxy.py, 默认关闭) ──
# mitmproxy>=10,<12
