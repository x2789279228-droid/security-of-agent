id: POL-LATERAL-MOVE
name: 横向移动主机隔离
enabled: true
description: 横向移动：隔离主机（需审批）
match:
  threat_type: LATERAL_MOVE
  min_confidence: 0.6
  min_severity: high
actions:
- name: isolate_host
  params: {}
- name: send_alert
  params:
    severity: critical
auto_execute: false
require_approval: true
priority: 85
cooldown_minutes: 60
