{
  "title": "Tempo APM — 链路追踪",
  "uid": "tempo-apm",
  "schemaVersion": 39,
  "version": 2,
  "tags": ["otel", "tempo", "trace"],
  "timezone": "browser",
  "editable": true,
  "panels": [
    {
      "id": 1,
      "type": "tempo",
      "title": "Trace Service Graph",
      "datasource": { "type": "tempo", "uid": "tempo" },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 0 },
      "targets": [
        {
          "queryType": "serviceMap",
          "serviceMap": { "datasource": { "type": "prometheus", "uid": "prometheus" } },
          "refId": "A"
        }
      ]
    },
    {
      "id": 2,
      "type": "table",
      "title": "Recent Traces",
      "datasource": { "type": "tempo", "uid": "tempo" },
      "gridPos": { "h": 10, "w": 12, "x": 0, "y": 8 },
      "targets": [
        {
          "queryType": "traceql",
          "query": "{}",
          "limit": 50,
          "tableType": "traces",
          "refId": "A"
        }
      ]
    },
    {
      "id": 3,
      "type": "table",
      "title": "Important/Audit Traces (soc.importance = important)",
      "datasource": { "type": "tempo", "uid": "tempo" },
      "gridPos": { "h": 10, "w": 12, "x": 12, "y": 8 },
      "targets": [
        {
          "queryType": "traceql",
          "query": "{ .soc.importance = \"important\" }",
          "limit": 50,
          "tableType": "traces",
          "refId": "A"
        }
      ],
      "description": "全量导出下用 soc.importance 属性筛选告警/审计关键链路；backend(pipeline_tracer/kafka_consumer/HttpTraceMiddleware) 与 flink(TraceUtil) 均已打标，见 docs/otel_sampling.md。"
    }
  ],
  "refresh": "10s",
  "time": { "from": "now-1h", "to": "now" }
}
