// Kafka Broker JAAS 配置 (SASL_SSL 监听器, SCRAM-SHA-512)
// 服务端仅需声明 ScramLoginModule; 用户凭证存放在 Broker 内部
// (__scram 主题), 由 tools/create-kafka-users.sh 创建, 本文件不含密钥, 可入库。
KafkaServer {
    org.apache.kafka.common.security.scram.ScramLoginModule required;
};
